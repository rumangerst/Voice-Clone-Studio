# Voice Clone Studio - Pinned Requirements (from current venv)
# Generated from requirements.txt package list, using versions currently installed in venv.
# This file intentionally excludes packages not listed in requirements.txt.

# Core TTS and audio processing dependencies (all platforms)
numpy==1.26.4
qwen-tts==0.1.1
librosa==0.11.0
torchaudio==2.9.1+cu130
soundfile==0.13.1
mutagen==1.47.0
sox==1.5.0
einops==0.8.2
huggingface_hub==0.36.2
onnxruntime==1.24.4
onnxruntime-gpu==1.24.4; sys_platform != 'darwin'

# Web UI and utilities
gradio==6.7.0
gradio_filelister @ file:wheel/gradio_filelister-0.4.0-py3-none-any.whl
diffusers==0.37.1
markdown==3.10.2
tensorboard==2.20.0

# MMAudio Sound Effects dependencies (torchvision installed with PyTorch in setup script)
open_clip_torch==3.3.0
torchdiffeq==0.2.5
timm==1.0.26
colorlog==6.10.1
omegaconf==2.3.0
av==17.0.0

# Fish Speech S2 dependencies (vendored in modules/fish_speech/)
# descript-audio-codec and descript-audiotools are installed with --no-deps
# in setup scripts to avoid audiotools' overly restrictive protobuf<3.20 pin.
hydra-core==1.3.2
loguru==0.7.3
protobuf==7.34.1
argbind==0.3.9
pyloudnorm==0.2.0
julius==0.2.7
ffmpy==1.0.0
#Missing Fish Speech dependence on new / clean install
flatten-dict==0.4.2
importlib-resources==6.5.2
tabulate==0.10.0
randomname==0.2.1
markdown2==2.5.5
matplotlib==3.10.8
torch-stoi==0.2.3
pystoi==0.4.1
ipython==9.10.1
#for compilation speed-up
triton-windows==3.6.0.post26; sys_platform == 'win32'
ninja==1.13.0

# LuxTTS voice cloning (optional - install via setup script or manually)
# Prerequisites (install in order before zipvoice):
#   pip install git+https://github.com/ysharma3501/LinaCodec.git
#   pip install piper-phonemize --find-links https://k2-fsa.github.io/icefall/piper_phonemize.html
#   pip install "zipvoice @ git+https://github.com/ysharma3501/LuxTTS.git"

# 4-bit quantization support (all platforms)
bitsandbytes==0.49.2

# Voice isolation and denoising (optional - requires Rust compiler to build from source)
# DeepFilterNet is installed separately by setup scripts with graceful fallback
# pip install deepfilternet

# Chatterbox Voice Conversion & TTS (vendored in modules/chatterbox/)
s3tokenizer==0.3.0
conformer==0.3.2

# VibeVoice LoRA training
peft==0.18.1

# Optional performance enhancements (install separately for best performance):
# - Flash Attention 2 (fast): pip install flash-attn --no-build-isolation

# Note for Linux users:
# - If onnxruntime fails to install, try the nightly build:
# - openai-whisper is excluded due to compatibility issues (llvmlite/numba build failures)
# - VibeVoice ASR can be used for transcription instead
